def gnis_uri(x):
    "Return a URI for a USGS Geoname."
    return 'http://data.usgs.gov/gnis/' + x

def structures_uri(x):
    "Return a URI for a USGS Structure."
    return 'http://data.usgs.gov/lod/topo/structures/' + x

def geonames_uri(x):
    "Return a URI for Geonames.org."
    return 'http://data.usgs.gov/geonames/' + x

def wfs_structures_uri(x):
	"Return a URI for WFS USGS Strucutres."
	return 'http://data.usgs.gov/USGS_TNM_Structures/' + x

def trails_uri(x):
    "Return a URI for USGS Trails."
    return 'http://data.usgs.gov/trails/' + x

def countryorequivalent_uri(x):
    "Return a URI for USGS CountyOrEquivalent."
    return 'http://data.usgs.gov/countyorequivalent/' + x

def stateorterritory_uri(x):
    "Return a URI for USGS StateOrTerritory."
    return 'http://data.usgs.gov/stateorterritory/' + x

def padus_uri(x):
    "Return a URI for USGS PADUS."
    return 'http://data.usgs.gov/padus/' + x

def nhdarea_uri(x):
    "Return a URI for USGS NHDArea."
    return 'http://data.usgs.gov/nhdarea/' + x

def nhdwaterbody_uri(x) :
    "Return a URI for USGS NHDWaterbody."
    return 'http://data.usgs.gov/nhdwaterbody/' + x

def nhdline_uri(x):
    "Return a URI for USGS NHDLine."
    return 'http://data.usgs.gov/nhdline/' + x

def nhdflowline_uri(x):
    "Return a URI for USGS NHDFlowline."
    return 'http://data.usgs.gov/nhdflowline/' + x

def nhdpoint_uri(x):
    "Return a URI for USGS NHDPoint."
    return 'http://data.usgs.gov/nhdpoint/' + x

def contours_uri(x):
    "Return a URI for USGS Contours."
    return 'http://data.usgs.gov/lod/contours/' + x
