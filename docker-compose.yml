version: '3'
services:
    tomcat:
        build: "./makb-tomcat/Dockerbuild"
        ports:
            - "8080:8080"
            - "2222:22"
        tty: true
    postgresql:
        build: "./makb-postgresql/Dockerbuild"
        ports:
            - "5432:5432"