version: '3.2'
services:
    tomcat:
        environment:
            - BRANCH
        build: "./makb-tomcat/Dockerbuild"
        ports:
            - "8080:8080"
            - "2222:22"
        tty: true
    postgresql:
        build: "./makb-postgresql/Dockerbuild"
        ports:
            - "5432:5432"